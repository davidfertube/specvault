{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp-config.schema.json",
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-filesystem", "/Users/david/.claude-worktrees/knowledge_tool/mystifying-keller"],
      "description": "File system access for SteelIntel project"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub integration for PRs and issues"
    }
  },
  "tools": {
    "allowed": [
      "read_file",
      "write_file",
      "list_directory",
      "search_files",
      "create_pull_request",
      "list_issues"
    ]
  }
}
